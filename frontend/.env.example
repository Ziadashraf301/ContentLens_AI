# Optional: Set to the full backend URL (default: same origin)
# Leave empty to use relative API endpoints (recommended in production behind a proxy)
REACT_APP_API_URL=

# Enable marketing workflow UI/features (true/false). Default: false
REACT_APP_ENABLE_MARKETING_WORKFLOW=false